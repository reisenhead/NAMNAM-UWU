<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.namnam_uwu">

    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_uwu_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_uwu_logo_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.NAMNAMUWU">
        <activity android:name=".UI.PhoneActivity"></activity>
        <activity android:name=".UI.PhoneActivity2" />
        <activity android:name=".UI.MyOrder" />
        <activity
            android:name=".Data.AddData"
            android:exported="true" />
        <activity android:name=".API.MainActivityApi" />
        <activity android:name=".UI.CrearCuenta" />

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".UI.HomeActivity"
            android:exported="true" />
        <activity
            android:name=".UI.LoginScreen"
            android:exported="true" />
        <activity
            android:name=".UI.RegisterScreen"
            android:exported="true"
            android:label="@string/title_activity_register_screen"
            android:theme="@style/Theme.NAMNAMUWU.NoActionBar" />
        <activity
            android:name=".UI.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>